{
  "buildCommand": "npm install --legacy-peer-deps && npm run build:routes && tsdown --config ./tsdown-vercel.config.ts",
  "outputDirectory": "src",
  "framework": null,
  "installCommand": "npm install --legacy-peer-deps"
}
